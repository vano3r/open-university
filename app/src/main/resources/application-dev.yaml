spring:
  security:
    user:
      name: admin@mail.ru
      password: admin
  datasource:
    url: jdbc:postgresql://localhost:5432/university_db
    username: un_admin
    password: un_password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true